---
title: "Get started with spatPatClassifyR"
vignette: >
  %\VignetteIndexEntry{get-started}
  %\VignetteEngine{quarto::html}
  %\VignetteEncoding{UTF-8}
knitr:
  opts_chunk:
    collapse: true
    comment: '#>'
---

There are two main workflows to classify landscape patterns using the `spatPatClassifyR` package: Either using landscape metrics as features for classification, or using convolutional neural networks (CNNs) to classify patterns directly from the pixel information of landscape images.

## The classification workflow

The general workflow consists of the following steps:

1. **Define the pattern classes** to distinguish. The classification is supervised, so the classes are chosen by the user and should be guided by the research question.
2. **Prepare training landscapes** with known patterns. These can either be artificial landscapes created with `create_landscapes()` / `create_landscape()`, or empirical landscapes wrapped into a landscape object with `landscape()`.
3. **Choose a workflow**: the *metric-based* approach (interpretable and effective with relatively few training landscapes) or the *pixel-based* approach (uses the raw pixel information from the landscapes but typically needs more training data).
4. **Calculate and select landscape metrics** (metric-based workflow only) with `calculate_metrics()` and `evaluate_metrics()`.
5. **Train a classifier** on the selected metrics with `train_metrics_model()`, or directly on the landscape pixels with `train_pixel_model()`. At this step the user can also validate the model using cross-validation methods.
6. **Classify new landscapes** with `apply_metrics_model()` or `apply_pixel_model()`, and inspect the results with `plot_classified_landscapes()`.

The diagram below summarizes these steps and how the two workflows relate.

```{=html}
<style>
.workflow-card {
  background: #fcfcfb;
  border: 1px solid rgba(0, 0, 0, 0.08);
  border-radius: 8px;
  padding: 1rem;
  margin: 1rem 0;
  overflow-x: auto;
}
.workflow-card svg { max-width: 100%; height: auto; }
.workflow-card code { white-space: nowrap; }
.workflow-card .edgeLabel, .workflow-card .edgeLabel * { color: #3f3f46 !important; }
</style>
```

::: {.workflow-card}
```{mermaid}
%%| label: fig-workflow
%%| fig-cap: "The **spatPatClassifyR** workflow. Steps shown in grey are shared by both workflows, while the cyan lane shows the metric-based approach and the orange lane the pixel-based approach. User decisions have a dashed outline, and arrows are labelled with the object type passed from one function to the next."
%%{init: {'theme':'base','flowchart':{'wrappingWidth':320},'themeVariables':{'fontFamily':'system-ui, -apple-system, Segoe UI, sans-serif','lineColor':'#57534e','edgeLabelBackground':'#fcfcfb','textColor':'#0b0b0b'}}}%%
flowchart TB
  A(["Define pattern classes"]):::userstep
  B["Generate or import landscapes<br/><code>create_landscapes()</code> · <code>create_landscape()</code> · <code>landscape()</code>"]:::shared
  A --> B
  B -->|landscape objects| D{"Choose workflow"}:::userstep

  subgraph MET ["Metric-based workflow"]
    M1["Calculate metrics<br/><code>calculate_metrics()</code>"]:::metrics
    M2["Select informative metrics<br/><code>evaluate_metrics()</code>"]:::metrics
    MP["Plot metric distributions<br/><code>plot_metrics()</code>"]:::metrics
    M3["Train classifier<br/><code>train_metrics_model()</code>"]:::metrics
    M4["Classify new landscapes<br/><code>apply_metrics_model()</code>"]:::metrics
    M1 -->|metric table| M2
    M2 -.-> MP
    M2 -->|selected metrics| M3
    M3 -->|trained model| M4
  end

  subgraph PIX ["Pixel-based workflow"]
    P1["Train classifier<br/><code>train_pixel_model()</code>"]:::pixel
    P2["Classify new landscapes<br/><code>apply_pixel_model()</code>"]:::pixel
    P1 -->|trained model| P2
  end

  D --> M1
  D --> P1
  M2 ~~~ P1
  B -.->|landscape objects| M4
  B -.->|landscape objects| P2
  M4 -->|prediction table| Z["Plot classification results<br/><code>plot_classified_landscapes()</code>"]:::shared
  P2 -->|prediction table| Z

  classDef shared   fill:#efedeb,stroke:#57534e,color:#0b0b0b;
  classDef metrics  fill:#d5efef,stroke:#008B8B,color:#0b0b0b;
  classDef pixel    fill:#ffedd5,stroke:#c2410c,color:#0b0b0b;
  classDef userstep fill:#ffffff,stroke:#57534e,stroke-dasharray:5 3,color:#0b0b0b;
  style MET fill:#f1fafa,stroke:#008B8B,color:#008B8B;
  style PIX fill:#fff8f2,stroke:#c2410c,color:#c2410c;
```
:::

Detailed guides for each workflow are provided in the corresponding vignettes:

- [Classify landscapes using landscape metrics](classify-metrics.html)
- [Classify landscapes using Keras on landscape rasters](classify-pixels.html)

There are also more detailed vignettes on specific landscape-related topics:

- [Importing user-defined landscapes](importing-landscapes.html)
- [Landscape generation](landscape-generation.html)