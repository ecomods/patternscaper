% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_params.R
\name{pattern_spots}
\alias{pattern_spots}
\title{Parameters for the Spots Pattern}
\usage{
pattern_spots(
  n_spots = 5,
  spot_radius = 10,
  spot_radius_sd = 0,
  radius_noise_fraction = 0,
  regular_spots = FALSE,
  invert_landscape = FALSE
)
}
\arguments{
\item{n_spots}{Integer. Number of spots (default: 5). Regular placement may
reduce this number if the landscape cannot accommodate the requested
count with the given \code{spot_radius}.}

\item{spot_radius}{Numeric. Mean radius of each spot in pixels (default: 10).
Must be positive and smaller than landscape dimensions.}

\item{spot_radius_sd}{Numeric. Standard deviation of normally sampled spot
radii (default: 0, no variation).}

\item{radius_noise_fraction}{Numeric. Fraction of the spot radius with a
gradual edge transition (0-1). Zero gives sharp edges; 1 applies
probabilistic cell inclusion across the full radius. For example, 0.2
applies the transition to the outer 20\%. This is independent of
\code{spot_radius_sd}, which varies overall spot size.}

\item{regular_spots}{Logical. Arrange spots on a hexagonal grid using k-means
clustering rather than placing them randomly (default: FALSE).}

\item{invert_landscape}{Logical. Create bare patches in vegetated ground,
equivalent to the "gaps" pattern (default: FALSE).}
}
\value{
A named \code{"landscape_params"} list containing the supplied
    parameters.
}
\description{
Circular vegetation patches on bare ground.
}
\examples{
# A single landscape, with fixed values
create_landscape("spots", params = pattern_spots(n_spots = 15))

# A batch, with parameters drawn from ranges once per landscape.
# Parameters left unset vary too, over their default ranges
create_landscapes(
  n = 4,
  patterns = "spots",
  params_list = list(
    spots = pattern_spots(
      n_spots = c(5, 15),
      spot_radius = c(4, 8)
    )
  )
)

# A batch, mixing a fixed value with a range
create_landscapes(
  n = 4,
  patterns = "spots",
  params_list = list(
    spots = pattern_spots(
      n_spots = 10,
      spot_radius = c(4, 8)
    )
  )
)

}
\seealso{
Other landscape creation:
\code{\link[=create_landscape]{create_landscape()}},
\code{\link[=create_landscapes]{create_landscapes()}},
\code{\link[=pattern_bands]{pattern_bands()}},
\code{\link[=pattern_bare]{pattern_bare()}},
\code{\link[=pattern_clustered]{pattern_clustered()}},
\code{\link[=pattern_dense]{pattern_dense()}},
\code{\link[=pattern_diffuse]{pattern_diffuse()}},
\code{\link[=pattern_fingers]{pattern_fingers()}},
\code{\link[=pattern_gaps]{pattern_gaps()}},
\code{\link[=pattern_labyrinth]{pattern_labyrinth()}},
\code{\link[=pattern_random]{pattern_random()}},
\code{\link[=pattern_sharp]{pattern_sharp()}}
}
\concept{landscape creation}
\section{Valid values and batch sampling}{
A single value fixes a parameter. A length-2 vector is a range, sampled
once per landscape by \code{\link{create_landscapes}} and rejected by
\code{\link{create_landscape}}. The table below shows for each parameter: 

\emph{valid} values that \code{\link{create_landscape}} accepts, and
\emph{sampled} ranges that \code{\link{create_landscapes}} draws from
once per landscape for any parameter left unset. The defaults shown in
Usage therefore apply to \code{\link{create_landscape}} only. Ranges that
scale with landscape size are shown for the default 100 by 100.
\tabular{lll}{
\strong{Parameter} \tab \strong{Valid} \tab \strong{Sampled} \cr
\code{n_spots} \tab 1 or more \tab 5 to 9 \cr
\code{spot_radius} \tab 1 or more \tab 10 to 15, scales with size \cr
\code{spot_radius_sd} \tab 0 or more \tab 0 to 2, scales with size \cr
\code{radius_noise_fraction} \tab 0 to 1 \tab 0 to 0.2 \cr
\code{regular_spots} \tab TRUE or FALSE \tab TRUE or FALSE \cr
\code{invert_landscape} \tab TRUE or FALSE \tab always FALSE \cr
}
}
