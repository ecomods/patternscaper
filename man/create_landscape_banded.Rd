% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/banded_landscapes.R
\name{create_landscape_banded}
\alias{create_landscape_banded}
\title{Create a Landscape with banded vegetation along a hill line ("tiger striped vegetation")}
\usage{
create_landscape_banded(
  width = 100,
  height = 100,
  hilltop = c(30, 20, 25),
  slope = c(0.2, 0.1, 0.3),
  nbands = 7,
  x_ext_hill = c(1.7, 2, 1.3),
  y_ext_hill = c(1.2, 1, 1.6),
  noise_sd = 0.1,
  rotation = 0,
  seed = 42,
  as_raster = TRUE,
  crs = NULL,
  add_metadata = TRUE
)
}
\arguments{
\item{width}{Integer. Number of columns in the landscape.}

\item{height}{Integer. Number of rows in the landscape.}

\item{hilltop}{List of Numerics/Integers. Elevation of hilltop.}

\item{slope}{List of Numerics. Slopes of each hill.}

\item{nbands}{Integer. Number of bands at the hill, one band is vegetated plus bare stripe.}

\item{x_ext_hill}{List of Numerics. Extention/distortion of a hill into x-direction}

\item{y_ext_hill}{List of Numerics. Extention/distortion of a hill into y-direction}

\item{noise_sd}{Numeric. If random effects, which standard deviation}

\item{rotation}{Numeric. Degrees of rotation to apply (counterclockwise). Default is 0 (no rotation).}

\item{seed}{Integer or NULL. Random seed for reproducibility (default: 42).
If NULL, a random seed based on system time will be used, producing different landscapes on each call.
If a specific integer is provided, the same landscape will be generated on repeated calls with that seed.}

\item{as_raster}{Logical. Whether to return as SpatRaster or a matrix (default: TRUE).}

\item{crs}{Character. Coordinate reference system (default: NULL).}

\item{add_metadata}{Logical. Whether to include metadata in output (default: TRUE).}
}
\value{
A matrix representing the landscape with banded vegetation, where 1 indicates vegetation and 0 indicates bare soil.
}
\description{
Generates a landscape with banded vegetation of plants and bare soil.
the strips are perpendicular to the slope of the hill.
The landscape can optionally be rotated.
}
