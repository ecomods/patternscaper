% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_create_curvyfingers.R
\name{create_landscape_curvyfingers}
\alias{create_landscape_curvyfingers}
\title{Create a Landscape with Curvy Fingers Treeline}
\usage{
create_landscape_curvyfingers(
  width = 100,
  height = 100,
  treeline_position = 0.5,
  random_spots = c(0, 0),
  sine_length_mean = 20,
  sine_length_sd = 12,
  sine_height_mean = 5,
  sine_height_sd = 4,
  rotation = 0
)
}
\arguments{
\item{width}{Integer. Width of the landscape in pixels (default: 100).}

\item{height}{Integer. Height of the landscape in pixels (default: 100).}

\item{treeline_position}{Numeric. Relative position of treeline from top (0-1) (default: 0.5).}

\item{sine_length_mean}{Numeric. Mean wavelength of sinusoidal curve in pixels (default: 20).}

\item{sine_length_sd}{Numeric. Standard deviation of wavelength of sinusoidal curve in pixels (default: 20).}

\item{sine_height_mean}{Numeric. Mean amplitude of sinusoidal curve in pixels (default: 5).}

\item{sine_height_sd}{Numeric. Standard deviation of  amplitude of sinusoidal curve in pixels (default: 5).}

\item{rotation}{Numeric. Angle to rotate landscape in degrees (default: 0).}
}
\value{
A landscape object with pattern "curvy" containing the generated landscape data and parameters.
}
\description{
Generates a binary landscape with a curvy finger treeline following a sine wave pattern
with random lenght of each sine wave and random height
}
\examples{
# Default curvy treeline
curvy_default <- create_landscape_curvy_treeline()

# Modified curvy treeline with increased sine parameters
curvy_modified <- create_landscape_curvy_treeline(
  treeline_position = 0.3,
  sine_length_mean = 40,
  sine_length_sd = 0,
  sine_height_mean = 10,
  sine_height_sd = 0
)

# With rotation and random waves
curvy_rotated <- create_landscape_curvy_treeline(
  treeline_position = 0.6,
  sine_length_mean = 10,
  sine_length_sd = 6,
  sine_height_mean = 6,
  sine_height_sd = 4,
  rotation = 45
)

}
\keyword{internal}
