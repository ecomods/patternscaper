% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_clustered.R
\name{create_landscape_clustered_trees}
\alias{create_landscape_clustered_trees}
\title{Create a Landscape with Clustered Trees}
\usage{
create_landscape_clustered_trees(
  width = 100,
  height = 100,
  treeline_position = 0.5,
  num_clusters = 10,
  cluster_radius = 5,
  scatter_zone_prop = 0.3,
  elongation_x = 1,
  elongation_y = 1,
  seed = NULL,
  rotation = 0,
  as_raster = TRUE,
  crs = NULL,
  add_metadata = TRUE
)
}
\arguments{
\item{width}{Integer. Width of the landscape in pixels (default: 100).}

\item{height}{Integer. Height of the landscape in pixels (default: 100).}

\item{treeline_position}{Numeric. Relative position of treeline from top (0-1) (default: 0.5).}

\item{num_clusters}{Integer. Number of cluster centers (default: 10).}

\item{cluster_radius}{Numeric. Radius of clusters in pixels (default: 5).}

\item{scatter_zone_prop}{Numeric. Proportion of height for scatter zone (default: 0.3).}

\item{elongation_x}{Numeric. Horizontal elongation factor for clusters (default: 1).}

\item{elongation_y}{Numeric. Vertical elongation factor for clusters (default: 1).}

\item{seed}{Integer or NULL. Random seed for reproducibility (default: NULL).
If NULL, no seed is set explicitly.
If a specific integer is provided, the same landscape will be generated on repeated calls with that seed.}

\item{rotation}{Numeric. Angle to rotate landscape in degrees (default: 0).}

\item{as_raster}{Logical. Whether to return as SpatRaster or a matrix (default: TRUE).}

\item{crs}{Character. Coordinate reference system (default: NULL).}

\item{add_metadata}{Logical. Whether to include metadata in output (default: TRUE).}
}
\value{
List with landscape (SpatRaster or Matrix) and metadata or only landscape without metadata)
}
\description{
Generates a binary landscape with clustered trees below a treeline.
}
\examples{
# Default clustered trees
clustered_default <- create_landscape_clustered_trees()

# Modified clustered trees with more elongated clusters and fixed seeed
clustered_modified <- create_landscape_clustered_trees(
  treeline_position = 0.2,
  num_clusters = 8,
  cluster_radius = 7,
  scatter_zone_prop = 0.6,
  elongation_x = 2.5,
  elongation_y = 0.5,
  seed = 42
)

# With rotation and random seed
clustered_rotated <- create_landscape_clustered_trees(
  num_clusters = 20,
  cluster_radius = 2,
  scatter_zone_prop = 0.5,
  elongation_x = 1.8,
  elongation_y = 1.4,
  rotation = 45
)
}
