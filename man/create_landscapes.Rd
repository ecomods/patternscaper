% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_create.R
\name{create_landscapes}
\alias{create_landscapes}
\title{Create Multiple Landscapes}
\usage{
create_landscapes(
  n = 50,
  patterns = c("random", "bare", "dense", "sharp", "diffuse", "fingers", "clustered",
    "bands", "spots", "gaps", "labyrinth"),
  width = 100,
  height = 100,
  rotation = 0:360,
  params_list = NULL,
  pattern_probs = NULL,
  max_retries = 3
)
}
\arguments{
\item{n}{Integer. Total number of landscapes to create (default: 50).}

\item{patterns}{Character vector. patterns of landscapes to sample from (default: all patterns).}

\item{width}{Integer. Width of all landscapes in pixels (default: 100).}

\item{height}{Integer. Height of all landscapes in pixels (default: 100).}

\item{rotation}{Numeric vector. Rotation angles in degrees (default: c(0, 45, 90, 135)).}

\item{params_list}{List. List of parameter ranges or single values for the parameters of each landscape pattern (default: NULL).
The names and default parameter ranges for the different patterns can be found
in the documentation of \code{\link{create_landscape}}.}

\item{pattern_probs}{Numeric vector. Probability that a specific landscape pattern is chosen from the list
of patterns. Should be a numeric vector of the same length as 'patterns'.
The default value NULL creates equally balanced patterns.}

\item{max_retries}{Integer. Maximum number of retries for failed landscape generations (default: 3).}
}
\value{
A named list of landscape objects. Names indicate the pattern and optional rotation.
}
\description{
Create a series of landscape models with variations.
Creates a total of n landscapes distributed across different landscape patterns.
}
\examples{
# Generate 20 landscapes
landscapes <- create_landscapes(n = 20)

# Access a landscape
landscapes[[1]]

# Check the pattern
landscapes[[1]]$pattern

# Get all landscape patterns
sapply(landscapes, function(x) x$pattern)

# Custom parameters for spot patterns and sharp vegetation boundary
# Can be given as a range (min, max) or a single value.
pattern_params <- list(
  spots = list(
    n_spots = 15,
    spot_radius = 10,
    spot_radius_sd = 3
  ),
  sharp = list(
    boundary_position = c(0.4,0.6)
))
landscapes_custom <- create_landscapes(
  n = 12,
 patterns = c("spots", "sharp"),
 params_list = pattern_params
)

}
\seealso{
Other landscape creation: 
\code{\link{create_landscape}()},
\code{\link{create_landscape_bands}()},
\code{\link{create_landscape_bare}()},
\code{\link{create_landscape_clustered}()},
\code{\link{create_landscape_dense}()},
\code{\link{create_landscape_diffuse}()},
\code{\link{create_landscape_fingers}()},
\code{\link{create_landscape_gaps}()},
\code{\link{create_landscape_labyrinth}()},
\code{\link{create_landscape_random}()},
\code{\link{create_landscape_sharp}()},
\code{\link{create_landscape_spots}()}
}
\concept{landscape creation}
