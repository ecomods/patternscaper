% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_create_stripes.R
\name{create_landscape_stripes}
\alias{create_landscape_stripes}
\title{Create a Landscape with striped vegetation along a hill line ("tiger striped vegetation")}
\usage{
create_landscape_stripes(
  width = 100,
  height = 100,
  nhills = 2,
  regular_hilltop = TRUE,
  top_elevation_mean = 30,
  top_elevation_sd = 2,
  slope_mean = 0.2,
  slope_sd = 0.05,
  nbands = 7,
  x_ext_hill_sd = 0.4,
  y_ext_hill_sd = 0.4,
  noise_sd = 0.1,
  rotation = 0
)
}
\arguments{
\item{width}{Integer. Number of columns in the landscape. Default is 100.}

\item{height}{Integer. Number of rows in the landscape. Default is 100.}

\item{nhills}{Integer. Number of hills in the landscape. Default is 2.}

\item{regular_hilltop}{Boolean. Regular or random position of the hilltops. Default is TRUE.}

\item{top_elevation_mean}{Numeric. Mean elevation of hilltops. Default is 30.}

\item{top_elevation_sd}{Numeric. Standard deviation of hilltop elevations. Default is 2.}

\item{slope_mean}{Numeric. Mean slope of hills 0.2 (per pixel). Default is 0.2.}

\item{slope_sd}{Numeric. Standard deviation of slope. Default is 0.05.}

\item{nbands}{Integer. Number of vegetation bands per hill. Default is 7.}

\item{x_ext_hill_sd}{Numeric. Standard deviation of extension of slope into x direction. Default is 0.4.}

\item{y_ext_hill_sd}{Numeric. Standard deviation of extension of slope into y direction. Default is 0.4.}

\item{noise_sd}{Numeric. Standard deviation for random elevation effects. Default is 0.1.}

\item{rotation}{Numeric. Degrees of rotation to apply (counterclockwise). Default is 0 (no rotation).}
}
\value{
A landscape object with pattern "stripes" containing the generated landscape data and parameters.
}
\description{
Generates a landscape with stripes vegetation of plants and bare soil.
the strips are perpendicular to the slope of the hill.
The landscape can optionally be rotated.
}
\examples{
# Default striped vegetation
stripes_default <- create_landscape_stripes()

# Modified striped vegetation with more stripes and different hill parameters
stripes_modified <- create_landscape_stripes(
  nhills = 2,
  nbands = 5,
  slope_mean = 0.5,
  regular_hilltop = FALSE,
  noise_sd = 0.5
)

# With rotation
stripes_rotated <- create_landscape_stripes(
  nhills = 3,
  nbands = 7,
  regular_hilltop = TRUE,
  noise_sd = 0,
  rotation = 45
)
}
\keyword{internal}
