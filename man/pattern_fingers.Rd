% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_params.R
\name{pattern_fingers}
\alias{pattern_fingers}
\title{Parameters for the Fingers Pattern}
\usage{
pattern_fingers(
  boundary_position = 0.5,
  noise_veg_to_bare = 0,
  noise_bare_to_veg = 0,
  sine_length_mean = 20,
  sine_length_sd = 12,
  sine_height_mean = 5,
  sine_height_sd = 4
)
}
\arguments{
\item{boundary_position}{Numeric. Relative position of the vegetation boundary
from the top (0-1, default: 0.5).}

\item{noise_veg_to_bare}{Numeric. Probability of flipping a vegetated cell to
bare, adding small gaps within the vegetation (0-1, default: 0).}

\item{noise_bare_to_veg}{Numeric. Probability of flipping a bare cell to
vegetated, scattering some vegetated cells beyond the vegetation boundary
(0-1, default: 0).}

\item{sine_length_mean}{Numeric. Mean wavelength of sinusoidal curve in pixels (default: 20).}

\item{sine_length_sd}{Numeric. Standard deviation of wavelength in pixels (default: 12).}

\item{sine_height_mean}{Numeric. Mean amplitude of sinusoidal curve in pixels (default: 5).}

\item{sine_height_sd}{Numeric. Standard deviation of amplitude in pixels (default: 4).}
}
\value{
A named list of the supplied parameters of the class \code{"landscape_params"}.
}
\description{
Finger-like extensions of vegetation growing into the bare zone.
Builds a validated parameter list for the \code{"fingers"} pattern, to pass
to \code{\link{create_landscape}} or \code{\link{create_landscapes}}.
}
\examples{
# A single landscape, with fixed values
create_landscape(
  "fingers",
  params = pattern_fingers(sine_length_mean = 15, sine_height_mean = 10)
)

# A batch, with parameters drawn from ranges once per landscape.
# Parameters left unset vary too, over their default ranges
create_landscapes(
  n = 4,
  patterns = "fingers",
  params_list = list(
    fingers = pattern_fingers(
      sine_length_mean = c(10, 30),
      sine_height_mean = c(5, 15)
    )
  )
)

# A batch, mixing a fixed value with a range
create_landscapes(
  n = 4,
  patterns = "fingers",
  params_list = list(
    fingers = pattern_fingers(
      sine_length_mean = 20,
      sine_height_mean = c(5, 15)
    )
  )
)

}
\seealso{
Other landscape creation:
\code{\link[=create_landscape]{create_landscape()}},
\code{\link[=create_landscapes]{create_landscapes()}},
\code{\link[=pattern_bands]{pattern_bands()}},
\code{\link[=pattern_bare]{pattern_bare()}},
\code{\link[=pattern_clustered]{pattern_clustered()}},
\code{\link[=pattern_dense]{pattern_dense()}},
\code{\link[=pattern_diffuse]{pattern_diffuse()}},
\code{\link[=pattern_gaps]{pattern_gaps()}},
\code{\link[=pattern_labyrinth]{pattern_labyrinth()}},
\code{\link[=pattern_random]{pattern_random()}},
\code{\link[=pattern_sharp]{pattern_sharp()}},
\code{\link[=pattern_spots]{pattern_spots()}}
}
\concept{landscape creation}
\section{Valid values and batch sampling}{
A single value fixes a parameter. A length-2 vector is a range, sampled
once per landscape by \code{\link{create_landscapes}} and rejected by
\code{\link{create_landscape}}.

\emph{Valid} is what \code{\link{create_landscape}} accepts.
\emph{Sampled} is the range \code{\link{create_landscapes}} draws from
per landscape for any parameter left unset, so the defaults shown in Usage
apply to \code{\link{create_landscape}} only. Ranges that scale with
landscape size are shown for the default 100 by 100.
\tabular{lll}{
\strong{Parameter} \tab \strong{Valid} \tab \strong{Sampled} \cr
\code{boundary_position} \tab 0 to 1 \tab 0.3 to 0.6 \cr
\code{noise_veg_to_bare} \tab 0 to 1 \tab not sampled \cr
\code{noise_bare_to_veg} \tab 0 to 1 \tab not sampled \cr
\code{sine_length_mean} \tab greater than 0 \tab 20 to 50, scales with size \cr
\code{sine_length_sd} \tab 0 or more \tab 10 to 50, scales with size \cr
\code{sine_height_mean} \tab 0 or more \tab 5 to 20, scales with size \cr
\code{sine_height_sd} \tab 0 or more \tab 5 to 25, scales with size \cr
}
}
