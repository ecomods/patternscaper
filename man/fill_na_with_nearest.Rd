% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotate_crop_landscape.R
\name{fill_na_with_nearest}
\alias{fill_na_with_nearest}
\title{Fill NA Values in a Matrix Using Nearest Neighbor Interpolation}
\usage{
fill_na_with_nearest(matrix_with_na, binarize = TRUE)
}
\arguments{
\item{matrix_with_na}{A numeric matrix containing NA values to be filled.}

\item{binarize}{Logical. If TRUE (default), the output matrix will be binarized,
with values < 0.5 set to 0 and values >= 0.5 set to 1. If FALSE, the interpolated
values are returned as is.}
}
\value{
A numeric matrix with NA values filled using nearest neighbor interpolation.
  If \code{binarize = TRUE}, the matrix will contain only 0s and 1s.
}
\description{
This function fills NA values in a matrix by applying nearest neighbor interpolation
row-wise sing the \code{na.approx} function from the \code{zoo} package.
Optionally, the function can binarize the resulting matrix based on a threshold of 0.5.
}
\details{
The function applies \code{zoo::na.approx} with \code{rule = 2} to ensure that
  NA values at the edges are filled with the nearest non-NA value. The interpolation
  is performed first row by row and then column by column to ensure all NA values are filled.
}
