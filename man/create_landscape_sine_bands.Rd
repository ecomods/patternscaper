% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/banded_landscapes.R
\name{create_landscape_sine_bands}
\alias{create_landscape_sine_bands}
\title{Create a Landscape with Sine Wave Bands}
\usage{
create_landscape_sine_bands(
  width = 100,
  height = 100,
  treeline_position = 0.5,
  band_thickness = 3,
  band_spacing = 10,
  frequency = 2 * pi/100,
  amplitude = 5,
  noise = FALSE,
  noise_sd = 1,
  rotation = 0,
  seed = NULL,
  as_raster = TRUE,
  crs = NULL,
  add_metadata = TRUE
)
}
\arguments{
\item{width}{Integer. Width of the landscape in pixels (default: 100).}

\item{height}{Integer. Height of the landscape in pixels (default: 100).}

\item{treeline_position}{Numeric. Relative position of treeline from top (0-1) (default: 0.5).}

\item{band_thickness}{Integer. Thickness of each band in pixels (default: 3).}

\item{band_spacing}{Integer. Spacing between bands in pixels (default: 10).}

\item{frequency}{Numeric. Frequency of sine wave (default: 2*pi/100).}

\item{amplitude}{Numeric. Amplitude of sine wave in pixels (default: 5).}

\item{noise}{Logical. Whether to add random noise to bands (default: FALSE).}

\item{noise_sd}{Numeric. Standard deviation for random noise (default: 1).}

\item{rotation}{Numeric. Angle to rotate landscape in degrees (default: 0).}

\item{seed}{Integer. Random seed for reproducibility (default: NULL).}

\item{as_raster}{Logical. Whether to return as SpatRaster or a matrix (default: TRUE).}

\item{crs}{Character. Coordinate reference system (default: NULL).}

\item{add_metadata}{Logical. Whether to include metadata in output (default: TRUE).}
}
\value{
List with landscape (SpatRaster or Matrix) and metadata or only landscape without metadata)
}
\description{
Generates a binary landscape with parallel sine-wave bands.
}
