% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_params.R
\name{pattern_clustered}
\alias{pattern_clustered}
\title{Parameters for the Clustered Pattern}
\usage{
pattern_clustered(
  boundary_position = 0.5,
  noise_veg_to_bare = 0,
  noise_bare_to_veg = 0,
  n_clusters = 10,
  cluster_radius = 5,
  scatter_zone_prop = 0.3,
  elongation_x = 1,
  elongation_y = 1
)
}
\arguments{
\item{boundary_position}{Numeric. Relative position of treeline from top
(0-1, default: 0.5).}

\item{noise_veg_to_bare}{Numeric. Probability of flipping a vegetated cell
to bare, adding gaps within the vegetation. Applied to the underlying
sharp treeline, before the clusters are placed (0-1, default: 0).}

\item{noise_bare_to_veg}{Numeric. Probability of flipping a bare cell to
vegetated, scattering vegetation beyond the treeline. Applied to the
underlying sharp treeline, before the clusters are placed (0-1,
default: 0).}

\item{n_clusters}{Integer. Number of cluster centers (default: 10).}

\item{cluster_radius}{Numeric. Radius of clusters in pixels (default: 5).}

\item{scatter_zone_prop}{Numeric. Proportion of height for scatter zone
measured downward from treeline (0-1, default: 0.3).}

\item{elongation_x}{Numeric. Horizontal elongation factor for clusters.
Values > 1 stretch clusters horizontally, creating wider ellipses.
Values < 1 compress horizontally (default: 1).}

\item{elongation_y}{Numeric. Vertical elongation factor for clusters.
Values > 1 stretch clusters vertically, creating taller ellipses.
Values < 1 compress vertically (default: 1).}
}
\value{
A named list of the supplied parameters, classed
    \code{"landscape_params"}.
}
\description{
Builds a validated parameter list for the \code{"clustered"} pattern, to
pass to \code{\link{create_landscape}} or \code{\link{create_landscapes}}.
}
\details{
A single value fixes a parameter. A length-2 vector is a range, sampled once
per landscape by \code{\link{create_landscapes}} and rejected by
\code{\link{create_landscape}}.
}
\examples{
create_landscape(
  "clustered",
  params = pattern_clustered(n_clusters = 8, cluster_radius = 7)
)

}
\seealso{
Other landscape creation:
\code{\link[=create_landscape]{create_landscape()}},
\code{\link[=create_landscapes]{create_landscapes()}},
\code{\link[=pattern_bands]{pattern_bands()}},
\code{\link[=pattern_bare]{pattern_bare()}},
\code{\link[=pattern_dense]{pattern_dense()}},
\code{\link[=pattern_diffuse]{pattern_diffuse()}},
\code{\link[=pattern_fingers]{pattern_fingers()}},
\code{\link[=pattern_gaps]{pattern_gaps()}},
\code{\link[=pattern_labyrinth]{pattern_labyrinth()}},
\code{\link[=pattern_random]{pattern_random()}},
\code{\link[=pattern_sharp]{pattern_sharp()}},
\code{\link[=pattern_spots]{pattern_spots()}}
}
\concept{landscape creation}
\section{Valid values and batch sampling}{
\emph{Valid} is what \code{\link{create_landscape}} accepts.
\emph{Sampled} is the range \code{\link{create_landscapes}} draws from
per landscape for any parameter left unset, so the defaults shown in Usage
apply to \code{\link{create_landscape}} only. Ranges that scale with
landscape size are shown for the default 100 by 100.
\tabular{lll}{
\strong{Parameter} \tab \strong{Valid} \tab \strong{Sampled} \cr
\code{boundary_position} \tab 0 to 1 \tab 0.4 to 0.6 \cr
\code{noise_veg_to_bare} \tab 0 to 1 \tab not sampled \cr
\code{noise_bare_to_veg} \tab 0 to 1 \tab not sampled \cr
\code{n_clusters} \tab 1 or more \tab 5 to 12 \cr
\code{cluster_radius} \tab 1 or more \tab 5 to 10 \cr
\code{scatter_zone_prop} \tab greater than 0, up to 1 \tab 0.2 to 1 \cr
\code{elongation_x} \tab greater than 0 \tab 0.5 to 1.5 \cr
\code{elongation_y} \tab greater than 0 \tab 0.5 to 1.5 \cr
}
}
