% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_params.R
\name{pattern_bands}
\alias{pattern_bands}
\title{Parameters for the Bands Pattern}
\usage{
pattern_bands(
  boundary_position = 0.5,
  band_zone_prop = 0.2,
  band_thickness = 3,
  band_spacing = 10,
  frequency = 2 * pi/100,
  amplitude = 5,
  noise_sd = 0
)
}
\arguments{
\item{boundary_position}{Numeric. Relative position of treeline from top
(0-1, default: 0.5).}

\item{band_zone_prop}{Numeric. Proportion of height of the total landscape to
allocate for bands below the treeline (default: 0.2). If the band zone is too small
for the given band spacing, no bands will be drawn and a warning will be issued.}

\item{band_thickness}{Integer. Thickness of each band in pixels (default: 3).}

\item{band_spacing}{Integer. Spacing between bands in pixels (default: 10).}

\item{frequency}{Numeric. Frequency of sine wave (default: 2*pi/100).}

\item{amplitude}{Numeric. Amplitude of sine wave in pixels (default: 5).}

\item{noise_sd}{Numeric. Standard deviation for random noise (default: 0).}
}
\value{
A named list of the supplied parameters, classed
    \code{"landscape_params"}.
}
\description{
Sinusoidal vegetation bands running parallel to the boundary. Builds a
validated parameter list for the \code{"bands"} pattern, to pass to
\code{\link{create_landscape}} or \code{\link{create_landscapes}}.
}
\details{
A single value fixes a parameter. A length-2 vector is a range, sampled once
per landscape by \code{\link{create_landscapes}} and rejected by
\code{\link{create_landscape}}.
}
\examples{
# A single landscape, with fixed values
create_landscape(
  "bands",
  params = pattern_bands(band_thickness = 4, band_spacing = 12)
)

# A batch, with parameters drawn from ranges once per landscape.
# Parameters left unset vary too, over their default ranges
create_landscapes(
  n = 4,
  patterns = "bands",
  params_list = list(
    bands = pattern_bands(
      band_thickness = c(2, 5),
      band_spacing = c(8, 16)
    )
  )
)

# A batch, mixing a fixed value with a range
create_landscapes(
  n = 4,
  patterns = "bands",
  params_list = list(
    bands = pattern_bands(
      band_thickness = 3,
      band_spacing = c(8, 16)
    )
  )
)

}
\seealso{
Other landscape creation:
\code{\link[=create_landscape]{create_landscape()}},
\code{\link[=create_landscapes]{create_landscapes()}},
\code{\link[=pattern_bare]{pattern_bare()}},
\code{\link[=pattern_clustered]{pattern_clustered()}},
\code{\link[=pattern_dense]{pattern_dense()}},
\code{\link[=pattern_diffuse]{pattern_diffuse()}},
\code{\link[=pattern_fingers]{pattern_fingers()}},
\code{\link[=pattern_gaps]{pattern_gaps()}},
\code{\link[=pattern_labyrinth]{pattern_labyrinth()}},
\code{\link[=pattern_random]{pattern_random()}},
\code{\link[=pattern_sharp]{pattern_sharp()}},
\code{\link[=pattern_spots]{pattern_spots()}}
}
\concept{landscape creation}
\section{Valid values and batch sampling}{
\emph{Valid} is what \code{\link{create_landscape}} accepts.
\emph{Sampled} is the range \code{\link{create_landscapes}} draws from
per landscape for any parameter left unset, so the defaults shown in Usage
apply to \code{\link{create_landscape}} only. Ranges that scale with
landscape size are shown for the default 100 by 100.
\tabular{lll}{
\strong{Parameter} \tab \strong{Valid} \tab \strong{Sampled} \cr
\code{boundary_position} \tab 0 to 1 \tab 0.3 to 0.5 \cr
\code{band_zone_prop} \tab 0 to 1 \tab 0.3 to 0.6 \cr
\code{band_thickness} \tab 1 or more \tab 2 to 4, scales with size \cr
\code{band_spacing} \tab 1 or more \tab 10 to 20, scales with size \cr
\code{frequency} \tab 0 or more \tab 0.1 to 0.3 \cr
\code{amplitude} \tab 0 or more \tab 0 to 6, scales with size \cr
\code{noise_sd} \tab 0 or more \tab 0 to 1, scales with size \cr
}
}
