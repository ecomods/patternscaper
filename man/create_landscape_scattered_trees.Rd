% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_create_scattered.R
\name{create_landscape_scattered_trees}
\alias{create_landscape_scattered_trees}
\title{Create a Landscape with Scattered Trees}
\usage{
create_landscape_scattered_trees(
  width = 100,
  height = 100,
  treeline_position = 0.5,
  scatter_density = 0.1,
  scatter_zone_prop = 0.2,
  random_spots = c(0, 0),
  rotation = 0,
  seed = NULL
)
}
\arguments{
\item{width}{Integer. Width of the landscape in pixels (default: 100).}

\item{height}{Integer. Height of the landscape in pixels (default: 100).}

\item{treeline_position}{Numeric. Relative position of treeline from top (0-1) (default: 0.5).}

\item{scatter_density}{Numeric. Probability of tree presence in the scatter zone (0-1) (default: 0.1).
Higher values result in a denser tree cover in the scatter zone.}

\item{scatter_zone_prop}{Numeric. Proportion of height for scatter zone (default: 0.5).
Defines how far below the treeline scattered trees can appear.}

\item{random_spots}{List of Numerics. Probability or random spots of vegetation in the other vegetation type (default: c(0,0))}

\item{rotation}{Numeric. Angle to rotate landscape in degrees (default: 0).}

\item{seed}{Integer or NULL. Random seed for reproducibility (default: NULL).
If NULL, no seed is set explicitly.
If a specific integer is provided, the same landscape will be generated on repeated calls with that seed.}
}
\value{
A landscape object with pattern "scattered" containing the generated landscape data and parameters.
}
\description{
Generates a binary landscape with randomly scattered trees below a treeline.
}
\examples{
scattered_default <- create_landscape_scattered_trees()

# Modified scattered trees with higher density in a larger scatter zone
scattered_modified <- create_landscape_scattered_trees(
  treeline_position = 0.3,
  scatter_density = 0.7,
  scatter_zone_prop = 0.2
)

# With rotation
scattered_rotated <- create_landscape_scattered_trees(
  treeline_position = 0.3,
  scatter_density = 0.2,
  scatter_zone_prop = 0.1,
  rotation = 45
)
}
\keyword{internal}
