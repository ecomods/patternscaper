% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_random.R
\name{create_landscape_random}
\alias{create_landscape_random}
\title{Create a Landscape with Randomly Distributed Trees}
\usage{
create_landscape_random(
  width = 100,
  height = 100,
  tree_prop = 0.5,
  rotation = 0,
  seed = NULL,
  as_raster = TRUE,
  crs = NULL,
  add_metadata = TRUE
)
}
\arguments{
\item{width}{Integer. Width of the landscape in pixels (default: 100).}

\item{height}{Integer. Height of the landscape in pixels (default: 100).}

\item{tree_prop}{Numeric. Probability of tree presence (0-1) (default: 0.5).
Higher values result in a denser tree cover.}

\item{rotation}{Numeric. Angle to rotate landscape in degrees (default: 0).}

\item{seed}{Integer or NULL. Random seed for reproducibility (default: NULL).
If NULL, no seed is set explicitly.
If a specific integer is provided, the same landscape will be generated on repeated calls with that seed.}

\item{as_raster}{Logical. Whether to return as SpatRaster or a matrix (default: TRUE).}

\item{crs}{Character. Coordinate reference system (default: NULL).}

\item{add_metadata}{Logical. Whether to include metadata in output (default: TRUE).}
}
\value{
List with landscape (SpatRaster or Matrix) and metadata or only landscape without metadata)
}
\description{
Generates a binary landscape with randomly distributed trees.
}
\examples{
# Default randomly distributed trees
random_default <- create_landscape_random()

# Modified random trees with higher density
random_modified <- create_landscape_random(
  tree_prop = 0.7
)

}
