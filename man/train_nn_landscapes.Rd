% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_keras.R
\name{train_nn_landscapes}
\alias{train_nn_landscapes}
\title{Train a Convolutional Neural Network for Landscape Classification}
\usage{
train_nn_landscapes(
  landscapes,
  cv_method = "k-fold",
  cv_folds = 5,
  epochs = 20,
  batch_size = 16,
  validation_split = 0.2,
  learning_rate = 0.001,
  model_path = NULL,
  seed = NULL
)
}
\arguments{
\item{landscapes}{List. List of landscape objects created by `create_landscape()` or `create_training_landscapes()`.}

\item{cv_method}{Character. Cross-validation method: "none", "k-fold" (default: "k-fold").}

\item{cv_folds}{Integer. Number of cross-validation folds when cv_method="k-fold" (default: 5).}

\item{epochs}{Integer. Number of training epochs (default: 20).}

\item{batch_size}{Integer. Batch size for training (default: 16).}

\item{validation_split}{Numeric. Proportion of data to use for validation when cv_method="none" (default: 0.2).}

\item{learning_rate}{Numeric. Learning rate for Adam optimizer (default: 0.001).}

\item{model_path}{Character. Path to save model (default: NULL means that the
model is not saved).}

\item{seed}{Integer. Random seed for reproducibility. If NULL, a random seed will be used (default: NULL).}
}
\value{
List. Trained CNN model and associated metadata.
}
\description{
Trains a CNN model using the Keras framework to classify landscapes based on their
spatial patterns. The function uses a multiscale CNN architecture optimized for
distinguishing different landscape patterns.
}
